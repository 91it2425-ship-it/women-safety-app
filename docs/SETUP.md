# Setup Instructions
To set up the Women's Safety App:
1. Clone the repository.
2. Navigate to the backend directory and run `mvn spring-boot:run`.
3. Open the Android project in Android Studio and run it.
